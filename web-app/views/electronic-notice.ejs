<!DOCTYPE html>
<html lang="ja">

<head>
    <%- include('partials/analytics') %>

        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>電子公告 - Stock with株式会社</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
        <style>
            .navbar-brand {
                font-family: 'Inter', 'Segoe UI', 'Roboto', 'Helvetica Neue', Arial, sans-serif;
                font-weight: 600;
                font-size: 1.5rem;
                text-transform: uppercase;
                letter-spacing: 0.5px;
                color: #1e40af !important;
                display: flex;
                align-items: center;
                line-height: 1;
            }

            .navbar-brand .brand-with {
                font-weight: 400;
                color: #1e40af;
            }

            .navbar-nav .nav-link {
                position: relative;
                transition: all 0.3s ease;
                display: flex;
                align-items: center;
                padding: 8px 16px;
                margin: 0 4px;
                color: #000000 !important;
                border-left: 1px solid rgba(0, 0, 0, 0.1);
                border-right: 1px solid rgba(0, 0, 0, 0.1);
            }

            .navbar-nav .nav-item:first-child .nav-link {
                border-left: none;
            }

            .navbar-nav .nav-item:last-child .nav-link {
                border-right: none;
            }

            .navbar-nav .nav-link::after {
                content: '';
                position: absolute;
                width: 0;
                height: 2px;
                bottom: 4px;
                left: 50%;
                background-color: #1e40af;
                transition: all 0.3s ease;
                transform: translateX(-50%);
            }

            .navbar-nav .nav-link:hover::after {
                width: calc(100% - 32px);
            }

            .navbar-nav .nav-link:hover {
                color: #1e40af !important;
            }

            .navbar-nav .nav-link i {
                font-size: 1.2rem;
            }

            .navbar-nav .nav-link .fa-instagram {
                color: #e1306c;
            }

            .navbar-nav .nav-link:hover .fa-instagram {
                color: #1e40af !important;
            }

            .navbar-nav .nav-item:has(.fa-instagram) .nav-link::after {
                content: '';
                position: absolute;
                width: 0;
                height: 2px;
                bottom: 4px;
                left: 50%;
                background-color: #1e40af;
                transition: all 0.3s ease;
                transform: translateX(-50%);
            }

            .navbar-nav .nav-item:has(.fa-instagram) .nav-link:hover::after {
                width: 20px;
            }

            .notice-card {
                transition: all 0.3s ease;
            }

            .notice-card:hover {
                transform: translateY(-2px);
                box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
            }

            .notice-type {
                position: relative;
                overflow: hidden;
            }

            .notice-type::before {
                content: '';
                position: absolute;
                top: 0;
                left: -100%;
                width: 100%;
                height: 100%;
                background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
                transition: left 0.5s;
            }

            .notice-type:hover::before {
                left: 100%;
            }

            body {
                padding-top: 76px;
            }
        </style>
</head>

<body class="bg-light">
    <!-- ナビゲーションバー -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm fixed-top">
        <div class="container">
            <a class="navbar-brand text-primary" href="/">
                <img src="/logo.png" alt="Stock with" height="40" class="me-2">
                STOCK&nbsp;<span class="brand-with">WITH</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="#about">
                            <i class="fas fa-info-circle me-1"></i>
                            ABOUT US
                        </a>
                    </li>
                    <li class="nav-item">
                        <button class="nav-link btn btn-link" onclick="window.Clerk.openSignIn()"
                            style="border: none; background: none; padding: 8px 16px;">
                            <i class="fas fa-right-to-bracket me-1"></i>
                            ログイン
                        </button>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link"
                            href="https://www.instagram.com/utsubo_zmoney?igsh=NTJsa3pkZnZzNXd1&utm_source=qr"
                            target="_blank" rel="noopener noreferrer" title="Instagram">
                            <i class="fab fa-instagram"></i>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-primary ms-3" href="/learning">学習を始める</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- メインコンテンツ -->
    <div class="container py-5">
        <!-- ページタイトル -->
        <div class="text-center mb-5">
            <h1 class="display-4 fw-bold text-primary mb-3">
                <i class="fas fa-bullhorn me-3"></i>電子公告
            </h1>
            <p class="lead text-muted">
                Stock with株式会社の重要な情報をお知らせいたします。定期的にご確認いただき、最新の情報をお見逃しなく。
            </p>
        </div>

        <!-- 検索・フィルター -->
        <div class="card mb-4">
            <div class="card-body">
                <div class="row g-3">
                    <div class="col-md-6">
                        <div class="input-group">
                            <span class="input-group-text">
                                <i class="fas fa-search"></i>
                            </span>
                            <input type="text" id="searchInput" class="form-control" placeholder="公告を検索...">
                        </div>
                    </div>
                    <div class="col-md-3">
                        <select id="typeFilter" class="form-select">
                            <option value="">すべての種類</option>
                            <option value="important">重要なお知らせ</option>
                            <option value="financial">財務情報</option>
                            <option value="business">事業報告</option>
                            <option value="legal">法的事項</option>
                            <option value="other">その他</option>
                        </select>
                    </div>
                    <div class="col-md-3">
                        <select id="yearFilter" class="form-select">
                            <option value="">すべての年</option>
                            <option value="2024">2024年</option>
                            <option value="2023">2023年</option>
                            <option value="2022">2022年</option>
                        </select>
                    </div>
                </div>
            </div>
        </div>

        <!-- 公告一覧 -->
        <div id="noticesContainer">
            <% if (notices && notices.length> 0) { %>
                <% notices.forEach(function(notice) { %>
                    <div class="notice-card card mb-4" data-type="<%= notice.type %>"
                        data-year="<%= new Date(notice.publishedAt).getFullYear() %>"
                        data-title="<%= notice.title.toLowerCase() %>"
                        data-content="<%= notice.content.toLowerCase() %>">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-8">
                                    <div class="d-flex align-items-center mb-2">
                                        <span class="notice-type badge me-2
                                            <%= notice.type === 'important' ? 'bg-danger' : 
                                                notice.type === 'financial' ? 'bg-success' :
                                                notice.type === 'business' ? 'bg-primary' :
                                                notice.type === 'legal' ? 'bg-warning' :
                                                'bg-secondary' %>">
                                            <i class="fas <%= notice.type === 'important' ? 'fa-exclamation-triangle' : 
                                                notice.type === 'financial' ? 'fa-chart-line' :
                                                notice.type === 'business' ? 'fa-briefcase' :
                                                notice.type === 'legal' ? 'fa-gavel' :
                                                'fa-info-circle' %> me-1"></i>
                                            <%= notice.type==='important' ? '重要なお知らせ' : notice.type==='financial'
                                                ? '財務情報' : notice.type==='business' ? '事業報告' : notice.type==='legal'
                                                ? '法的事項' : 'その他' %>
                                        </span>
                                        <small class="text-muted">
                                            <i class="fas fa-calendar-alt me-1"></i>
                                            <%= new Date(notice.publishedAt).toLocaleDateString('ja-JP', {
                                                year: 'numeric' , month: 'long' , day: 'numeric' }) %>
                                        </small>
                                    </div>
                                    <h5 class="card-title mb-2">
                                        <%= notice.title %>
                                    </h5>
                                    <p class="card-text text-muted">
                                        <%= notice.content.substring(0, 200) %>
                                            <%= notice.content.length> 200 ? '...' : '' %>
                                    </p>
                                    <% if (notice.attachments && notice.attachments.length> 0) { %>
                                        <small class="text-muted">
                                            <i class="fas fa-paperclip me-1"></i>
                                            <%= notice.attachments.length %>個の添付ファイル
                                        </small>
                                        <% } %>
                                </div>
                                <div class="col-md-4 text-md-end">
                                    <button onclick="toggleNotice('<%= notice.id %>')" class="btn btn-primary">
                                        <i class="fas fa-eye me-1"></i>詳細を見る
                                    </button>
                                </div>
                            </div>

                            <!-- 詳細表示エリア（初期状態では非表示） -->
                            <div id="notice-<%= notice.id %>" class="hidden border-top pt-3 mt-3">
                                <div class="card-text">
                                    <div class="whitespace-pre-wrap">
                                        <%= notice.content %>
                                    </div>
                                </div>
                                <% if (notice.attachments && notice.attachments.length> 0) { %>
                                    <div class="mt-3">
                                        <h6 class="fw-bold">
                                            <i class="fas fa-paperclip me-1"></i>添付ファイル
                                        </h6>
                                        <div class="list-group">
                                            <% notice.attachments.forEach(function(attachment) { %>
                                                <div
                                                    class="list-group-item d-flex justify-content-between align-items-center">
                                                    <div class="d-flex align-items-center">
                                                        <i class="fas fa-file-pdf text-danger me-2"></i>
                                                        <span class="fw-medium">
                                                            <%= attachment.name %>
                                                        </span>
                                                        <small class="text-muted ms-2">(<%= attachment.size %>)</small>
                                                    </div>
                                                    <a href="<%= attachment.url %>"
                                                        class="btn btn-sm btn-outline-primary" target="_blank">
                                                        <i class="fas fa-download me-1"></i>ダウンロード
                                                    </a>
                                                </div>
                                                <% }); %>
                                        </div>
                                    </div>
                                    <% } %>
                            </div>
                        </div>
                    </div>
                    <% }); %>
                        <% } else { %>
                            <div class="text-center py-5">
                                <i class="fas fa-inbox text-muted" style="font-size: 4rem;"></i>
                                <h3 class="mt-3 text-muted">公告がありません</h3>
                                <p class="text-muted">現在、表示する公告がありません。</p>
                            </div>
                            <% } %>
        </div>

        <!-- ページネーション -->
        <nav aria-label="公告ページネーション" class="mt-4">
            <ul class="pagination justify-content-center">
                <li class="page-item disabled">
                    <span class="page-link">
                        <i class="fas fa-chevron-left"></i>
                    </span>
                </li>
                <li class="page-item active">
                    <span class="page-link">1</span>
                </li>
                <li class="page-item">
                    <a class="page-link" href="#">2</a>
                </li>
                <li class="page-item">
                    <a class="page-link" href="#">3</a>
                </li>
                <li class="page-item">
                    <a class="page-link" href="#">
                        <i class="fas fa-chevron-right"></i>
                    </a>
                </li>
            </ul>
        </nav>
    </div>

    <!-- フッター -->
    <footer class="bg-dark text-white py-4 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <div class="d-flex align-items-center mb-2">
                        <img src="/logo.png" alt="Stock with" height="30" class="me-2">
                        <h5 class="mb-0">Stock with</h5>
                    </div>
                    <p class="text-muted">株式投資学習プラットフォーム</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <div class="mb-2">
                        <a href="/privacy" class="text-muted text-decoration-none me-3">
                            <i class="fas fa-shield-alt me-1"></i>プライバシーポリシー
                        </a>
                        <a href="/terms" class="text-muted text-decoration-none me-3">
                            <i class="fas fa-file-contract me-1"></i>利用規約
                        </a>
                        <a href="/commercial-disclosure" class="text-muted text-decoration-none me-3">
                            <i class="fas fa-balance-scale me-1"></i>特定商取引法に基づく表記
                        </a>
                        <a href="/notice" class="text-muted text-decoration-none me-3">
                            <i class="fas fa-bullhorn me-1"></i>電子公告
                        </a>
                        <a href="/admin/notice" class="text-muted text-decoration-none">
                            <i class="fas fa-cogs me-1"></i>管理画面
                        </a>
                    </div>
                    <p class="text-muted mb-0">&copy; 2024 Stock with株式会社. All rights reserved.</p>
                </div>
            </div>
        </div>
    </footer>

    <!-- JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        window.Clerk = window.Clerk || {};
        window.Clerk.publishableKey = '<%= publishableKey || "" %>';
    </script>
    <script>
        // 公告の詳細表示切り替え
        function toggleNotice(noticeId) {
            const noticeDetail = document.getElementById(`notice-${noticeId}`);
            const button = event.target;

            if (noticeDetail.classList.contains('hidden')) {
                noticeDetail.classList.remove('hidden');
                button.innerHTML = '<i class="fas fa-eye-slash me-1"></i>詳細を閉じる';
            } else {
                noticeDetail.classList.add('hidden');
                button.innerHTML = '<i class="fas fa-eye me-1"></i>詳細を見る';
            }
        }

        // 検索・フィルター機能
        document.addEventListener('DOMContentLoaded', function () {
            const searchInput = document.getElementById('searchInput');
            const typeFilter = document.getElementById('typeFilter');
            const yearFilter = document.getElementById('yearFilter');
            const noticesContainer = document.getElementById('noticesContainer');

            function filterNotices() {
                const searchTerm = searchInput.value.toLowerCase();
                const selectedType = typeFilter.value;
                const selectedYear = yearFilter.value;

                const notices = noticesContainer.querySelectorAll('.notice-card');

                notices.forEach(notice => {
                    const title = notice.dataset.title;
                    const content = notice.dataset.content;
                    const type = notice.dataset.type;
                    const year = notice.dataset.year;

                    const matchesSearch = !searchTerm ||
                        title.includes(searchTerm) ||
                        content.includes(searchTerm);

                    const matchesType = !selectedType || type === selectedType;
                    const matchesYear = !selectedYear || year === selectedYear;

                    if (matchesSearch && matchesType && matchesYear) {
                        notice.style.display = 'block';
                    } else {
                        notice.style.display = 'none';
                    }
                });
            }

            searchInput.addEventListener('input', filterNotices);
            typeFilter.addEventListener('change', filterNotices);
            yearFilter.addEventListener('change', filterNotices);
        });
    </script>
</body>

</html>